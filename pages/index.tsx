const Home = () => {
  const version: number = 34;

  return (
    <div>
      <h1>Custom Next JS & Typescript setup</h1>
      <p>Next Version: {version}</p>
    </div>
  );
};

export default Home;
